{
  "name": "ralph-loop-plugin",
  "version": "1.1.0",
  "description": "Fork: Minimal Ralph Loop plugin for opencode — auto-continues until task completion. Adds /ralph alias, RALPH_MAX_ITERATIONS env var, error recovery.",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "ralph-loop",
    "ralph-wiggum",
    "ralph",
    "ai-agent",
    "auto-continue",
    "iteration-loop"
  ],
  "author": "Charles Feng, fork by @edulelis",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/edulelis/ralph-loop-plugin"
  },
  "bugs": {
    "url": "https://github.com/edulelis/ralph-loop-plugin/issues"
  },
  "homepage": "https://github.com/edulelis/ralph-loop-plugin#readme",
  "files": [
    "src/",
    "commands/",
    "skills/"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": ">=1.0.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.6"
  }
}
